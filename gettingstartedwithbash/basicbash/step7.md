To go back to the parent directory, we can use the following command:

`cd ..`{{execute}}

Now lets check where we are:

`pwd`{{execute}}

Great stuff, lets clean up the directory before we finish here...